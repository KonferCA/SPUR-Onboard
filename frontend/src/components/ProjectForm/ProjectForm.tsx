import { FC } from 'react';
import type { ProjectFormProps } from '@/components/ProjectForm/types';

export const ProjectForm: FC<ProjectFormProps> = ({
    groupedQuestions,
    steps,
}) => {
    return <div></div>;
};
